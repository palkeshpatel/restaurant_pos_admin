'use client'

import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#FF9900',
      dark: '#F08700',
      light: '#FFB340',
    },
    secondary: {
      main: '#dc004e',
    },
  },
})

